// const { Router } = require("express");
// const responseHandler = require("../../utils/responseHandlers");
// const auth = require("../../middleware/auth");
// const { ContactUs, removeserviceFromCart, getcart, addcart } = require('./controller');
// const app = Router();

// // app.post("/cart-registration", auth, responseHandler(cartRegistration));

// app.post("/Contact-Us", auth, responseHandler(ContactUs));
// app.get("/get-cart", auth, responseHandler(getcart));
// app.get("/add-cart", auth, responseHandler(addcart))

// module.exports = app;